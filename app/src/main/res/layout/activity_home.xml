<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HomeActivity">

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginBottom="67dp"
        android:background="@color/bg_grey"
        />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/constraint_layouthome"
        android:layout_width="match_parent"
        android:layout_height="67dp"
        android:elevation="4dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        >
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/add_fab_button"
        android:layout_width="wrap_content"
        android:layout_height="56dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:foregroundGravity="center"
        android:elevation="10dp"
        android:layout_marginBottom="8dp"
        android:backgroundTint="@color/primary_color"
        android:src="@drawable/fab_add_24"
        app:tint="@android:color/white"
        />

    <ImageButton
        android:id="@+id/home_image_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginBottom="8dp"
        android:elevation="6dp"
        android:outlineSpotShadowColor="@color/white"
        android:background="@color/white"
        android:src="@drawable/home_filled"
        app:layout_constraintBottom_toBottomOf="@id/constraint_layouthome"
        app:layout_constraintStart_toStartOf="@id/constraint_layouthome" />

    <ImageButton
        android:id="@+id/myroom_image_button"
        android:layout_width="48dp"
        android:layout_height="56dp"
        android:src="@drawable/my_room_outlined"
        android:background="@color/white"
        android:elevation="6dp"
        android:outlineSpotShadowColor="@color/white"
        app:layout_constraintBottom_toBottomOf="@id/constraint_layouthome"
        app:layout_constraintEnd_toEndOf="@id/constraint_layouthome"
        android:layout_marginEnd="32dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>
</androidx.constraintlayout.widget.ConstraintLayout>